// reduction factor
// full, half and quarter are supported
// full: use every y value
// half: use every second y value (halves the output resolution)
// quarter: use every fourth y value (quarters the output resolution)
reduction = half;
reductionUpper = half;

// compute horizontal sobel for lower image
horizontal = false;

// compute vertical sobel for lower image
vertical = false;

// compute magnitude sobel for lower image
magnitude = true;

// compute horizontal sobel for upper image
horizontalUpper = false;

// compute vertical sobel for upper image
verticalUpper = false;

// compute magnitude sobel for upper image
magnitudeUpper = true;