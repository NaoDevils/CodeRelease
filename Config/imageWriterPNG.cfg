imageConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

sequenceImageConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

ballPerceptConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

processedballPatchesConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

robotsPerceptConfig = {
  enabled = false;          // bool to determine if the PNG-Logger is enabled or not
  upper = true;             // log from the upper image
  lower = true;             // log from the lower image
  enableProtobuf = true;    // whether to write a chunk with camera data etc.
  enableProjection = false; // bool to determine if Images robot estimates that are out of the images bounds are projected back into the image
  margin = 0;             // the fraction of the width around the patch that will be taken into account
};

penaltyCrossPatchesConfig = {
  enabled = false;         // bool to determine if the PNG-Logger is enabled or not
  upper = true;            // log from the upper image
  lower = true;            // log from the lower image
  enableProtobuf = true;   // whether to write a chunk with camera data etc.
  enableProjection = true; // bool to determine if Images robot estimates that are out of the images bounds are projected back into the image
  margin = 0.0;            // the fraction of the width around the patch that will be taken into account
};

useGroundTruthData = false;
datasetName = "";      // a name to group the files afterwards
datasetNameBlacklist = [
  Logs,
  bigLogs
];
