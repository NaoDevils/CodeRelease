imageConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

sequenceImageConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

ballPerceptConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

processedballPatchesConfig = {
  enabled = false;       // bool to determine if the PNG-Logger is enabled or not
  upper = true;          // log from the upper image
  lower = true;          // log from the lower image
  enableProtobuf = true; // whether to write a chunk with camera data etc.
};

useGroundTruthData = false;
datasetName = "";      // a name to group the files afterwards
datasetNameBlacklist = [
  Logs,
  bigLogs
];